name: common_flutter_start
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  http: ^1.1.2
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: 2.24.2
  firebase_database: ^10.3.8
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
